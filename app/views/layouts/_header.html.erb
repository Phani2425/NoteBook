<nav class="bg-white shadow">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="flex h-16 justify-between">
      <div class="flex">
        <div class="flex flex-shrink-0 items-center">
          <%= image_tag 'sticky-notes.png', class: 'w-12' %>
        </div>
      </div>
      <div class="hidden sm:ml-6 sm:flex sm:items-center justify-between gap-4">
        <% unless user_signed_in? %>
          <%= link_to 'Login',
            new_user_session_path,
            class: "w-full flex items-center justify-center px-5 py-2 border border-transparent text-base font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700" 
          %>
        <%else%>
          <%= link_to 'Logout',
            destroy_user_session_path,
            data: { "turbo-method": :delete },
            class: "w-full flex items-center justify-center px-5 py-2 border border-transparent text-base font-medium rounded-md text-indigo-700 bg-indigo-100 hover:bg-indigo-200"
          %>
        <%end%>
      </div>
    </div>
  </div>
</nav>