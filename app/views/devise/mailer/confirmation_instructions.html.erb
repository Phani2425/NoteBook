<div class="min-h-screen bg-gray-50 py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-md w-full mx-auto">
    <div class="bg-white p-8 border border-gray-200 shadow-sm rounded-lg">
      <div class="text-center mb-8">
        <h1 class="text-2xl font-bold text-gray-900 mb-2">Welcome to Notes App!</h1>
        <p class="text-gray-600"><%= @email %></p>
      </div>

      <div class="space-y-6 text-gray-700">
        <p>Thank you for signing up. To complete your registration, please confirm your account by clicking the button below:</p>

        <div class="text-center">
          <%= link_to 'Confirm my account', 
              confirmation_url(@resource, confirmation_token: @token),
              class: "inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
        </div>

        <div class="text-sm text-gray-500">
          <p>If the button doesn't work, copy and paste this link into your browser:</p>
          <p class="mt-2 break-all"><%= confirmation_url(@resource, confirmation_token: @token) %></p>
        </div>

        <div class="border-t border-gray-200 pt-6 mt-6 text-sm text-gray-500">
          <p>If you didn't create an account, you can safely ignore this email.</p>
        </div>
      </div>
    </div>
  </div>
</div>